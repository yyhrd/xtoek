apiVersion: v1
kind: Service
metadata:
  name: cfoj
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "cfoj"
    port: 8080
  selector:
    app: cfoj
