apiVersion: v1
kind: Service
metadata:
  name: rzoh
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "rzoh"
    port: 8080
  selector:
    app: rzoh
