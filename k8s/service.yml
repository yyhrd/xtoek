apiVersion: v1
kind: Service
metadata:
  name: yygs
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "yygs"
    port: 8080
  selector:
    app: yygs
