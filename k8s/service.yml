apiVersion: v1
kind: Service
metadata:
  name: uuan
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "uuan"
    port: 8080
  selector:
    app: uuan
