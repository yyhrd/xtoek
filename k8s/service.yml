apiVersion: v1
kind: Service
metadata:
  name: yiwv
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "yiwv"
    port: 8080
  selector:
    app: yiwv
