apiVersion: v1
kind: Service
metadata:
  name: rair
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "rair"
    port: 8080
  selector:
    app: rair
