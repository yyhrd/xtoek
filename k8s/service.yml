apiVersion: v1
kind: Service
metadata:
  name: daibo
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "daibo"
    port: 8080
  selector:
    app: daibo
