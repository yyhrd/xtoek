apiVersion: v1
kind: Service
metadata:
  name: neyj
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "neyj"
    port: 8080
  selector:
    app: neyj
