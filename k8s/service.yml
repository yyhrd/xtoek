apiVersion: v1
kind: Service
metadata:
  name: zttk
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "zttk"
    port: 8080
  selector:
    app: zttk
