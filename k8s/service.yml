apiVersion: v1
kind: Service
metadata:
  name: mago
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "mago"
    port: 8080
  selector:
    app: mago
