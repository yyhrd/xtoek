apiVersion: v1
kind: Service
metadata:
  name: trww
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "trww"
    port: 8080
  selector:
    app: trww
