apiVersion: v1
kind: Service
metadata:
  name: csoo
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "csoo"
    port: 8080
  selector:
    app: csoo
