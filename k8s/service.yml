apiVersion: v1
kind: Service
metadata:
  name: mana
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "mana"
    port: 8080
  selector:
    app: mana
