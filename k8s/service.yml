apiVersion: v1
kind: Service
metadata:
  name: guog
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "guog"
    port: 8080
  selector:
    app: guog
