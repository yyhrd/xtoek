apiVersion: v1
kind: Service
metadata:
  name: yayu
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "yayu"
    port: 8080
  selector:
    app: yayu
